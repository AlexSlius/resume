import Head from 'next/head'
import { WrapperPage } from '../src/wrappers/pages'
import { CoverLetterTemplates } from '../src/containers/cover-letter-templates'
import { withPublicRoute } from "../src/middleware/publicRouter";

const ContactUs = () => {
    return (
        <>
            <Head>
                <title>Cover letter templates</title>
                <meta name="description" content="Generated by create next app" />
            </Head>
            <WrapperPage>
                <CoverLetterTemplates />
            </WrapperPage >
        </>
    )
}

export const getServerSideProps = withPublicRoute({ isGetCoverTemplates: true });

export default ContactUs;
